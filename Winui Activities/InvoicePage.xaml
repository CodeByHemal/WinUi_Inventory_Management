<Page
    x:Class="WinUi_Inventory_Management.Winui_Activities.InvoicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUi_Inventory_Management.Winui_Activities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:models="using:WinUi_Inventory_Management.Models"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="30">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontSize="{StaticResource TitleTextBlockFontSize}" FontWeight="SemiBold">
                    Invoices
                </TextBlock>
                <Border Width="60" Height="60" BorderBrush="White" BorderThickness="1" CornerRadius="60" Margin="0,0,0,10" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Image x:Name="ProfileImage" Source="/Assets/user_profile.png" Width="60" Height="60" />
                </Border>
            </Grid>

            <!-- Dynamic Invoice List -->
            <ListView Grid.Row="1" ItemsSource="{x:Bind Orders}" Margin="0,20,0,0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Order">
                        <Border
                            Width="800"
                            Padding="20"
                            Margin="10"
                            CornerRadius="12"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Image Source="/Assets/invoice_image.png" Grid.Column="0" Width="40" Height="40" />
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0" >
                                    <Run>Invoice_</Run>
                                    <Run Text="{x:Bind OrderName}"/>
                                    <Run>_</Run>
                                    <Run Text="{x:Bind CreatedAt}"/>
                                    <Run>.pdf</Run>
                                </TextBlock>
                                <Button Grid.Column="2" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Click="DownloadInvoice">
                                    <FontIcon Glyph="&#xE896;" FontSize="18"/>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </ScrollViewer>
</Page>
