<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUi_Inventory_Management.Winui_Activities.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUi_Inventory_Management.Winui_Activities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        
    
    <Grid Padding="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
    
        <TextBlock Grid.Column="0" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}">Settings</TextBlock>

        <Grid Grid.Row="2" Margin="0,30,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="auto"  />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

                <Button Click="OnProfileImageClick" Background="Transparent" BorderThickness="0">
                    <Image x:Name="ProfileImage" Height="100" Width="100" />
                </Button>
                <TextBox x:Name="FullName" Grid.Row="1" Width="320" Header="Fullname" PlaceholderText="Hemal Rashiya" Margin="0,25,0,0"></TextBox>
            <TextBox x:Name="Email" Grid.Row="2" Width="320" Header="Email" PlaceholderText="hrasiya212@rku.ac.in" Margin="0,25,0,0"></TextBox>
            <TextBox x:Name="Password" Grid.Row="3" Width="320" Header="Password" PlaceholderText="Hemal@123" Margin="0,25,0,0"></TextBox>

            <Button x:Name="SaveProfile" Grid.Row="4" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Margin="0,25,0,0">Save Profile</Button>
            <Button Grid.Row="5"  HorizontalAlignment="Stretch" Margin="0,25,0,0" Foreground="{StaticResource SystemFillColorCritical}">Logout</Button>


        </Grid>
        
    </Grid>
    </ScrollViewer>
</Page>
